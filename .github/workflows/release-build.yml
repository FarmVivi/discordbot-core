name: Release Build

permissions:
  contents: write   # pour uploader l’artefact sur la release

on:
  release:
    types: [ created ]

jobs:
  build-jar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@v5.6.0
        with:
          java-version: '25'
          distribution: "temurin"
          cache: maven

      # Cache Maven packages
      - name: Cache Maven packages
        uses: actions/cache@v6.1.0
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # Étape pour enlever "-SNAPSHOT" dans le pom.xml (multi-module)
      - name: Remove -SNAPSHOT from pom.xml (all modules)
        run: mvn versions:set -DremoveSnapshot -DgenerateBackupPoms=false -DprocessAllModules

      # Construction Maven avec optimisations
      - name: Build with Maven
        run: mvn -T1C package -DskipTests -Dmaven.test.skip=true -Dmaven.javadoc.skip=true

      # On récupère le vrai nom du .jar (ex: fluxcord-core-1.2.3.jar) depuis le module core
      - name: Find built jar name
        id: find_jar
        shell: bash
        run: echo "jar_name=$(basename $(ls fluxcord-core/target/fluxcord-core-*.jar))" >> $GITHUB_OUTPUT

      # Upload du .jar sur la release GitHub
      - name: Upload JAR to release
        uses: softprops/action-gh-release@v3.0.2
        with:
          files: fluxcord-core/target/${{ steps.find_jar.outputs.jar_name }}
